<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body class="container">

    <header>
        <%- include('../partials/header'); %>
    </header>

    <main>
        <div class="item-container horse-container"></div>
        <script>getHorse(<%= id %>)</script>
        <% if (isAdmin) { %>
            <button type="button" class="btn btn-danger" onclick="deleteHorse(<%= id %>)">Delete</button>
        <% } %>
    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>
</html>